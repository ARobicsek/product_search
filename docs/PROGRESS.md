# Progress

**This is the live status file. Every dev session reads it first and updates it last.**
Keep it small (see [SESSION_PROTOCOL.md](SESSION_PROTOCOL.md) — hard cap, archive-on-close).
Full session-by-session history → [PROGRESS_ARCHIVE.md](PROGRESS_ARCHIVE.md) (append-only, NOT read at session start).

## Active phase

- **Closed:** Phases 0–16; **Phase 17** (schedule editor + alerts); **Phase 19** (universal adapter accuracy & vendor reach); **Phase 20** (reliable scheduling trigger); **Phase 22 — Recall reliability under degraded AlterLab + onboarder robustness** (ADR-078/079/080, 2026-05-24); **Phase 23 — Hybrid filter restoration + headless E2E verification** (both Parts A and B closed, 2026-05-24); **Phase 24 — Vendor-quirks coverage audit + Amazon JS-render fix** (ADR-082, 2026-05-24); **Phase 25 — "Explain the zero": classified source-outcome reasons + AlterLab 422 transient retry** (ADR-083/084, 2026-05-24); **Phase 26 — Cross-cutting LIVE stress test & regression sweep** (findings in [STRESS_TEST_26.md](STRESS_TEST_26.md), 2026-05-24); **Phase 27 — Fix the 3 Phase 26 defects + live re-verify** (ADR-085, verification in [STRESS_TEST_27.md](STRESS_TEST_27.md), 2026-05-25).
- **ACTIVE ARC — the rebuild (ADR-133, owner-signed [REBUILD_PLAN.md](REBUILD_PLAN.md)): Serper recall + eBay API + Haiku filter, "keep core, redesign edges."** Phase 30 (spike → GO, ADR-131; filter-model = Haiku-4.5@temp=0, ADR-132) DONE. **Phase 31 (Listing generalize + profile v2 + Serper adapter + Serper-aware `single_sku_url` + temp=0) DONE 2026-05-30 (ADR-134).** NEXT = Phase 33 (eBay wire-in + vendor allow/blocklist). Phase 32 (price-sanity/ship-from gate + diversity selection + type-aware display + honest error taxonomy + v2 run spine) DONE 2026-05-30 (ADR-135). Sequencing 31→36 in REBUILD_PLAN §10.
- **Phase 29 — Recall & reproducibility overhaul** (WOUND DOWN — superseded by the rebuild; ADR-105 through ADR-125 shipped; brief in [PHASES.md](PHASES.md)). Session-C defect queue (ADR-117/118/119/120) archived to [PROGRESS_ARCHIVE.md](PROGRESS_ARCHIVE.md) — revisit only if it survives the new scope. The DONE bullets below are Phase-29/30 lineage, kept for reference.
  - **DONE 2026-05-27: ADR-105 (registry-driven search URLs) + ADR-109 (honest per-source diagnostics)** — the highest-leverage pair for the Microcenter mis-scope class.
  - **DONE 2026-05-27: ADR-106 (parser-gap recall recovery)** — deterministic embedded-state tier for Walmart's `__NEXT_DATA__` grid.
  - **DONE 2026-05-27: ADR-108 (onboard prompt diet)** — `onboard_v1.txt` cut 737→461 lines (−44% by words). See "Most recent work" below.
  - **DONE 2026-05-27: ADR-110** — Onboarder turn budget enforced via `turn-budget.ts`, explicit tool-less finalize, client-side graceful recovery message on interruption. Fixes the silent freeze failure mode.
  - **DONE 2026-05-27: ADR-108 verification** — 3× DJI onboard via Chrome DevTools MCP against prod (`ari-product-search.vercel.app/onboard`). All 3 runs finalized perfectly with no silent timeouts, confirming ADR-110 and ADR-108 stability live.
  - **DONE 2026-05-27: ADR-107** — generalized auto-Scrappey to known-good thin-body vendors. The universal_ai adapter now automatically falls back to Scrappey after AlterLab escalation if it encounters a bot-wall (sub-15KB body) on a vendor marked as `alterlab_known_good`, ensuring that recall is preserved on tough targets without wasting Scrappey credits on normal fetchers.
  - **DONE 2026-05-28: ADR-111 (Session A)** — hard-gated `force_detail_backup` at save (`validateProfileDraft` routes the check to `errors`, not `warnings`), fixed the Save-button-stuck-disabled bug (`OnboardChat.onSubmit` resets `saveState` on follow-up turns), updated the onboarder prompt so `validate_profile` errors are understood to BLOCK save (LLM must fix-and-revalidate, not surface to user). 6 new guard tests; web `tsc`/`eslint`/`next build` clean; worker 412/412.
  - **DONE 2026-05-28: ADR-114** — server-streams `<draft>` JSON to the right pane so the onboard preview stays accurate during long tool-use loops (fixes the "right pane shows `{}` despite 8 vendors added" defect).
  - **DONE 2026-05-28: ADR-115** — save-time probe modal with *Continue probing* / *Save and proceed anyway* and `bypassForceDetailBackup` flag. Final piece for the "force-finalize stranded the user" failure class.
  - **DONE 2026-05-28 (Session C): ADR-121** — Probe modal bounded loop + de-duped backfill rows + plan visibility. Originally PROPOSED as a P3 cosmetic; Session-C code review found two real functional bugs (the loop genuinely never terminated for slow/walled vendors; backfill rows accumulated one-per-attempt because the client never keyed by host). Server now emits `noProgress` in the `done` event when a Continue pass finishes zero URLs; client hides *Continue probing* and offers an explicit **Stop and save what we have** CTA. Backfill events go through a host-keyed `upsertBackfill` so re-skipped vendors render once, not once-per-attempt. New `plan_summary` event seeds a per-host chip roll-up at the top of the modal. 4 new guard tests (35 total).
  - **DONE 2026-05-29: Redacted Google API keys from static test fixtures** — fixed the exposed secrets security alerts by replacing 13 public Google API keys with uniquely mapped dummy keys of matching length (39 characters) in 5 HTML fixture files. Entire test suite (413/413) remains green.
  - **DONE 2026-05-28: ADR-116 (P0)** — detail-URL relevance gate + match_aliases hallucination guard. New pure `detail-title-match.ts` (`titleMatchesTarget`, deterministic token-overlap — rejects the brand-only DJI Transceiver page, accepts siblings/terse titles) wired into `probe-url.ts` (`ProbeResult.detailTitleMatch`; `ok=false` + reason on mismatch). New pure `alias-hallucination-check.ts` (`checkMatchAliasesAgainstHallucinatedSkus` + `extractSkuTokens`) wired into `validation.ts` as a hard error — catches the exact `B0FJ1QH15P`-in-aliases signature. Prompt rule added + `sync-prompt.cjs` re-run. 11 new guard tests (46 total). Closes Standing Candidates #5 + #7.
  - **DONE 2026-05-29: test-slug leak fix** — `ai_filter` mirrors each run's verdicts to `reports/<slug>/<date>.filter.jsonl`; in tests that leaked `reports/test-product`, `reports/test-subscription`, `reports/ddr5-rdimm-256gb` into the working tree on every run. Added a `PRODUCT_SEARCH_REPORTS_DIR` env override (mirrors `PRODUCT_SEARCH_PRODUCTS_DIR`) honored by `_repo_reports_dir()`, plus an autouse session fixture in `conftest.py` pointing it at a tmp dir. Tests now write the per-product log to tmp; real `reports/` stays clean. Worker 413/413, ruff+mypy clean.
  - **DONE 2026-05-29: ADR-122** — onboard-probing UX (from a live DJI re-onboard): save-time probe now REUSES interview probes (chat route streams `probe_result`; `onSave` passes them as `priorResults`; `canReuse` skips re-probing confirmed URLs except force_detail_backup hosts needing a detail URL); probes SEQUENTIALLY fastest-first with a per-URL soft cap so progress is deterministic (was: parallel race → 5/6 "budget exhausted", 0 progress); deterministic "Keep probing the unfinished vendors" affordance on a `turn_truncated` chat-turn timeout; clearer budget-exhausted wording. 6 new guard tests (52 total).
  - **DONE 2026-05-29: ADR-123 (onboarder message clarity) + ADR-124 (run-diagnostic correctness)** — from a live DJI Neo 2 re-onboard + run. **ADR-123:** every save-time validator now carries a plain-English `userMessage` (+ "What to do:") alongside the technical `message`; the LLM still gets the technical text via `validate_profile`, the UI shows the friendly text. De-jargoned the probe-modal header; turned the tiny "Open <slug> anyway →" link into an amber button. +8 guard tests (60 total). **ADR-124:** fixed a real misclassification — `vendor_does_not_carry` (stamped on EVERY `fetched==0` source by `cli.annotate_dominant_rejections`) was checked FIRST in `classify_source_outcome`, making the carry-gate WATCHED + bot-wall/degraded TRANSIENT + PARSER_GAP branches dead code for universal sources. Moved it to last-resort; +5 regression tests (worker 418/418). This is what made the user's Amazon detail source (bot-walled, 0 fetched) report the misleading "Vendor doesn't carry — re-running won't help."
  - **DONE 2026-05-29: ADR-125 (Scrappey recovery for known-good detail pages + Amazon use_scrappey)** — answers "why didn't bot-walled Amazon fall back to Scrappey?" Two gaps: (1) the ADR-107 thin-body Scrappey fallback lived only in the search-union path; an explicit `page_type:detail` source returns `[]` before reaching it. (2) Amazon was `alterlab_known_good` without `use_scrappey`, and its bot-wall (200 + multi-KB body) doesn't trip `_weak_render_reason`. Fix: added a Scrappey recovery to the explicit-detail branch (thin body + known-good + not-already-scrappey → re-fetch + re-extract, `render_js:true`); flagged `amazon.com` `use_scrappey:true`+`render_js:true`+`proxy_country` in the registry (no `skip_alterlab`; AlterLab stays backup; propagates to existing profiles via merge). Repointed the old amazon ADR-107 test to bestbuy.com; +2 tests. Worker 420/420.
  - **DONE 2026-05-29: browser-MCP setup for web-session live testing** — the live-verify task was blocked because the web container had no browser-automation MCP and no vendor/LLM API keys. Fixed by committing `.mcp.json` (chrome-devtools MCP, headless/isolated) + `.claude/hooks/setup-browser.sh` (SessionStart-installs headless Chromium via Playwright, symlinks `/usr/local/bin/chrome-for-cdmcp`) + `.claude/settings.json`. Validated end-to-end this session (MCP init, 29 tools, `navigate_page`+`take_snapshot` on the prod `/onboard` UI). Two container gotchas baked into `.mcp.json`: `--acceptInsecureCerts` (the container does TLS interception → Chrome `ERR_CERT_AUTHORITY_INVALID`) and `--chrome-arg=--no-sandbox` (root). NOTE: MCP loads at session start, so this session can't use it — **the NEXT web session does the actual live test.**
  - **DONE 2026-05-29: live naive-user verification (DJI Neo 2) — findings in [STRESS_TEST_29.md](STRESS_TEST_29.md).** Drove prod end-to-end via Chrome DevTools MCP as a non-technical user (interview → 2 save-probes → save → Run-now → report). **ADR-122/123/124/125 all verified live and PASS:** ADR-124 (Amazon bot-walled/0-fetched reported `transient`+`needs work`, NOT "vendor doesn't carry") and ADR-125 (B&H Cloudflare bypassed via Scrappey → real $599 listing; Amazon detail fetched full 494KB via the Scrappey route) are confirmed. ADR-123 plain-English save messages + "Open my product page" button, ADR-122 probe-reuse/deterministic-progress/keep-probing affordance, ADR-121 modal polish, and ADR-116 ASIN guard (`B0FJ1QH15P` kept out of `match_aliases`) all working. **Three new real defects captured (NOT fixed — owner prioritizes):** **ADR-126 (P0/P1)** save-time probe silently demotes known-good vendors (Micro Center, Walmart) to `sources_pending` on transient 504s and never tells the user — defeats ADR-124 at the run layer (pending = never searched); **ADR-127 (P1)** ADR-116 alias guard misses B&H `CP.FP.00000273.01` product numbers (leaked into `match_aliases`); **ADR-128 (P1/P2)** Amazon detail renders 494KB via Scrappey but extractor parses 0 + maxes 8,192 output tokens (overlaps ADR-119/120). Plus UX paper-cuts (run-status auto-refresh + resetting timer, dangling "Continue probing" text, stale "Last run" metadata, silent long interview-turn probes). Env caveat: AlterLab/Scrappey looked degraded (persistent microcenter/walmart 504s).
  - **DONE 2026-05-29 (planning/strategy session): ADR-130 — Phase 30 recall-layer pivot scoped.** Owner asked for a strategic judgment (abandon / keep-fixing / new approach) after STRESS_TEST_29's poor recall. Conclusion: the filter/synth/no-fabrication stack works; *sourcing* is the broken layer, and self-scraping an adversarial web is an unwinnable treadmill. Proposed pivot: replace the self-scraping source layer with a **shopping-API recall primitive (Serper.dev)** + keep everything downstream. Serper key validated live this session (DJI Neo 2 → B&H/Walmart/Newegg; DDR5-256GB → Newegg/Best Buy/NEMIX/ServerSupply/CDW; Amazon absent from Google Shopping — known caveat). Key stored in local `.env` (gitignored); owner to add `SERPER_API_KEY` to GitHub Actions secrets (+ Vercel if the onboarder uses it). Full spike plan + GO/NO-GO gate: **[NEXT_SESSION_PHASE30_SPIKE.md](NEXT_SESSION_PHASE30_SPIKE.md)**. Step 2 coverage check DONE this session — **6/6 PASS**, see [STRESS_TEST_30.md](STRESS_TEST_30.md) (Step 3 filter-precision test still pending). On branch `claude/product-search-strategy-s26wo`.
  - **DONE 2026-05-29: Phase 30 spike Step 3 + GO/NO-GO call → ADR-131 (GO, with 3 prerequisites).** Fed real captured Serper shopping results (DDR5-RDIMM-256GB + DJI-Neo-2) through the **unmodified `ai_filter`** offline (fixture profiles via `PRODUCT_SEARCH_PRODUCTS_DIR`; ADR-062). DDR5 **37/40** pass (only self-identifying UDIMM rejected); DJI **25/31** (all DJI Neo v1 + Mini 2/SE rejected, every genuine Neo 2 kept). Wrong-product precision perfect; recall crushes STRESS_TEST_29 (1/4→0/4). **Verdict: GO**, gated on three finite prerequisites — (P0) `single_sku_url` must be Serper-aware (Serper's `link` is always a `google.com/search?` redirect → verbatim rejects 100%, Run A = 0/40); (P1) deterministic price-sanity + ship-from gate (anomalous/foreign offers leak, no detail page to self-correct); (P1/P2) title-only loses detail-page specs → ~1 sub-variant leak/40, intersects ADR-117 `variant_strict`. Method caveat: no `ANTHROPIC_API_KEY` in the web container, so only the `call_llm` network boundary was intercepted — every other line of `ai_filter` ran for real; verdicts apply the dumped prod prompt's rules (this session's model standing in for Haiku-4.5). Artifacts: `worker/scripts/serper_spike.py` + `serper_filter_runtest.py`, `worker/tests/fixtures/serper/*.json`, fixture profile `dji-neo-2-motion-fly-more-combo`. Full writeup: [STRESS_TEST_30.md](STRESS_TEST_30.md). On branch `claude/loving-darwin-LP3gY`.
  - **DONE 2026-05-29: Phase 30 Step 3b/3c — real-model runs + filter-model bake-off (keys provided).** Re-ran Step 3 against REAL models (no stand-in) + a gold-scored bake-off (`serper_filter_bakeoff.py`). **Headline: the run-to-run non-determinism was a TEMPERATURE bug** — `ai_filter` never sets `temperature` (runs at provider default ~1.0); live Haiku gave 35/28/19 on identical DDR5 input, but at **temp=0 both Haiku and GLM-4.6 are deterministic (Jaccard 1.0)**. Recommend a one-line `temperature=0` fix to the filter call regardless of migration. **Bake-off across 4 products (DDR5/DJI/book/subscription): precision perfect (1.00) for both models everywhere** (no-false-positive property holds on title-only Serper data); **recall is the variable** — GLM-4.6 wins DDR5 (0.76 vs 0.57), ties on the book (both flawlessly drop `[Used]` + wrong-title books), but **over-rejects the subscription** (3–4 vs Haiku's 8) and its **cost swings 10× ($0.0086→$0.0905/run)** on ambiguous catalogs (Haiku flat ~$0.017). **Owner chose GLM-4.6** — defensible @ temp=0, with guardrails (watch recall/cost on ambiguous products; Haiku is the flat-cost fallback; enriching filter input is the bigger recall lever). Full data in [STRESS_TEST_30.md](STRESS_TEST_30.md) Steps 3b/3c.
  - **DONE 2026-05-29: Phase 30 filter-model bake-off → ADR-132.** Full 4-model × 4-product bake-off at temp=0, 3 trials. **Filter model for the migrated build = Haiku-4.5 @ `temperature=0`** (only model precise on all four + wins the ambiguous subscription; GLM-4.6 disqualified — non-det + FP leak + cost spike on the subscription; GPT-4o-mini rejects everything; Gemini = cost fallback). Plus: set `temperature=0` in the prod `ai_filter` call. Table + reasoning in [STRESS_TEST_30.md](STRESS_TEST_30.md) "Step 3e". Run-and-read; no production code touched.
  - **DONE 2026-05-30: app-rebuild PLANNING session → ADR-133 + [REBUILD_PLAN.md](REBUILD_PLAN.md) (owner-signed).** End-to-end rebuild designed around Serper recall + eBay API + Haiku filter, "keep core (~70%), redesign edges." Anchoring principle: **ship Serper-only data / Google buy-links / no Amazon / eBay-on now, architect every seam for the rich future** (type-aware detail verification via `Listing.attrs`, merchant links via `Listing.buy_url`, Amazon via one-more-adapter). Profile collapses to "query + spec"; `Listing` generalized off RAM; onboarder v2 = conversational intake + ONE live Serper preview (retires the whole probe/backfill/save-modal apparatus). All forks owner-signed via two `AskUserQuestion` rounds (verify-depth, rebuild-scope, Amazon, buy-link, variant_strict, eBay-scope, breadth). No production code (planning only). Eval base unchanged: GO (ADR-131), Haiku-4.5@temp=0 (ADR-132). [NEXT_SESSION_REBUILD_PLANNING.md](NEXT_SESSION_REBUILD_PLANNING.md) was the brief; superseded by REBUILD_PLAN.md.
- **Also queued:** **Schedule & Alerts editor prod verification** (ADR-059/060/061). Deferred standing candidate.
- **Most recent work:** 2026-05-30 **Phase 32 IMPLEMENTED — v2 run pipeline works end-to-end (ADR-135).** Wired the v2 (Serper-recall) run spine + the deterministic edges (REBUILD_PLAN §10). New pure/deterministic modules (no LLM, ADR-001): `validators/price_sanity.py` (MAD-vs-median anomaly flags over **survivors** not raw recall — `price_anomaly_low/high`, conservative MAD+ratio gate — plus a ship-from/country gate; ADR-131 P1), `selection.py` (cheapest-first + `per_vendor_cap` + `max_listings`; excludes `price_anomaly_low` from display but keeps it in history so the $67.20 anomaly never ranks #1; per-vendor `overflow`), `run_outcome.py` (§8 taxonomy), `display_v2.py` (type-aware columns), `synthesizer/report_json_v2.py` (v2 JSON sidecar `schema_version:2` + lean markdown), `run_v2.py` (orchestration + pure injectable-`ai_filter` core `run_v2_pipeline`). **Filter reuse via `profile_v2_filter.py` shim** (v2→v1 `Profile` carrying only what `apply_filters`/`ai_filter` read; aliases fold into description; `min_quantity` NOT mapped → `degraded_attr` note; transitional, Phase 36 deletes it). `cli search` peeks `schema_version` → routes v2 to `run_v2`; v1 path untouched. +40 guard tests (worker **481/481**), ruff(src)+mypy clean. End-to-end CLI smoke verified. No web changes (UI render of `columns`/`overflow` = Phase 34).
- **Prior work:** 2026-05-30 **Phase 31 IMPLEMENTED — Serper recall works end-to-end (ADR-134).** Delivered all five REBUILD_PLAN §10 Phase-31 items. **`Listing` generalized ADDITIVELY** (owner approved the §3 deviation at session start via `AskUserQuestion`): `price_usd` **property** aliasing stored `unit_price_usd`; **kept** kit/qvl fields as optional; **added** `buy_url`/`image_url`/`rating`/`rating_count` (+ to_dict). Destructive rename deferred to Phase 36 (§3 blast radius = 234 + 331 refs across ~40 RAM-pipeline files Phase 36 deletes). **`profile_v2.py`** = SEPARATE `ProfileV2` model (required `schema_version: 2` discriminator; reuses v1 `Target`/`Schedule`/`_resolve_profile_path`; ports the distinctive-alias validator; `filters` is a typed `FiltersV2` object — minor §4 deviation noted in ADR-134). **`adapters/serper.py`** mirrors `adapters/ebay.py` (fixture + live `POST google.serper.dev/shopping`); `source="serper_shopping"`, `url`==`buy_url`==google-shopping redirect (no fabricated merchant URL), `condition=""` NOT `"unknown"` (the pitfall — `condition_in:[new]` would reject 100%), dedup by productId→link. **Serper-aware `single_sku_url`** (ADR-131 P0): `reject_single_sku_url` short-circuits for `serper_shopping`; ai_filter prompt got the exception + now sends `source` per listing. **`temperature=0`** (ADR-132): threaded through `call_llm`→3 providers (anthropic via SDK `omit` sentinel); `ai_filter` passes `temperature=0`. +21 guard tests + v2 fixture profile. **Worker 441/441, ruff(src)+mypy clean.** No web changes (onboarder v2 = Phase 34).
- **Prior work:** 2026-05-29 **Phase 30 filter-model bake-off → ADR-132 picks Haiku-4.5 @ temp=0.** Ran `serper_filter_bakeoff.py --all --trials 3 --temperature 0` (real calls to Haiku-4.5, GLM-4.6, GPT-4o-mini, Gemini-2.5-flash-lite) across DDR5/DJI/The-Week-subscription/Netanyahus-book; full 4×4 table + F1 matrix in [STRESS_TEST_30.md](STRESS_TEST_30.md) "Step 3e". **Recommendation: Haiku-4.5 @ `temperature=0`** — the only model with precision 1.00 on all four AND a win on the ambiguous subscription (P=R=det=1.00, flat cost). **GLM-4.6 (the prior Step-3c lean) disqualified as default** — on the subscription it went non-deterministic (det 0.79, counts [9,4,11]), leaked false positives (P 0.73 — breaks ADR-001), cost spiked 6×. **GPT-4o-mini unusable** (rejected 100% on DDR5 + subscription). **Gemini = cost fallback** (flawless precision/determinism, cheapest, but recall 0.38 subscription). Also accepted: thread `temperature=0` through the prod `ai_filter` call. No production code changed (run-and-read). ADR-132 logged.
- **Prior work:** 2026-05-29 **Phase 30 spike Step 3 → ADR-131 (GO).** Ran real Serper shopping results through the unmodified `ai_filter` offline; DDR5 37/40, DJI 25/31, wrong-product precision perfect, recall crushes STRESS_TEST_29. **GO on the migration**, gated on 3 finite prerequisites (Serper-aware `single_sku_url` [P0 — verbatim google-redirect links reject 100%], deterministic price-sanity/ship-from gate, title-only sub-variant precision / ADR-117). Scratch scripts + fixtures committed; worker `data/` (intercept dumps) gitignored. Develop branch: `claude/loving-darwin-LP3gY`.
- **Prior work:** 2026-05-29 **Phase 29 ADR-123 + ADR-124 + ADR-125** — human-readable onboarder save messages (separate from LLM-facing text); fixed the `vendor_does_not_carry` classifier ordering that made WATCHED/TRANSIENT/PARSER_GAP unreachable for zero-fetch universal sources; closed the Scrappey-fallback gaps for known-good *detail* pages + routed Amazon through Scrappey. Develop branch: `claude/quirky-volta-L3R7L` (NOT main this session — user is mid-investigation). Web `tsc`/`eslint`/`test:guards 60/60`/`test:parity 6/6`/`next build` green; worker `pytest 420/420`, ruff(src)+mypy clean.
- **Prior work:** 2026-05-29 **Phase 29 ADR-122: onboard-probing UX — reuse interview probes at save, deterministic progress, keep-probing affordance, clearer wording.**
  - **ADR-122:** Chat route (`app/api/onboard/chat/route.ts`) streams `probe_result` verdicts + a `turn_truncated` signal on force-finalize. Probe route (`app/api/onboard/probe/route.ts`) reuses confirmed interview verdicts (`canReuse`, re-probing force_detail_backup hosts still needing a detail URL so backfill keeps listings), probes sequentially fastest-first (`probeCost`) with `PER_URL_SOFT_CAP_MS=38000`, budget 45→50s. `OnboardChat.tsx`: `chatProbesRef` accumulates verdicts, `onSave` passes them as `priorResults`, reused vendors render ✓, deterministic "Keep probing the unfinished vendors" banner on `turn_truncated`, reworded `url_deadline` text. 6 new guard tests (52 total). Web `tsc`/`eslint`/`test:guards 52/52`/`test:parity 6/6`/`next build` green; worker untouched.
- **Prior work:** 2026-05-28 **Phase 29 ADR-116: detail-URL relevance gate + match_aliases hallucination guard.**
  - **ADR-116:** New pure module `detail-title-match.ts` — `titleMatchesTarget(title, targetName)` (deterministic token overlap via `familyRootTokens`; 1 token must match, ≥2 need ≥2). `probe-url.ts` `extractDetailListing` now returns the verbatim title; `probeUrl` computes `detailTitleMatch` for `page_type:"detail"` and sets `ok=false` + a "different product" reason when it is `false`. New pure module `alias-hallucination-check.ts` — `checkMatchAliasesAgainstHallucinatedSkus` hard-errors when an Amazon ASIN / B&H product-number lifted from a source URL appears in `match_aliases` (wired into `validation.ts` errors, so save 422s and ADR-113 auto-forwards to the LLM). Deviated from the proposal: deterministic verdict (not Haiku) + deterministic unit tests (not captured HTML/live Haiku) — both because the architecture distrusts the LLM for verification and the guard suite is offline. Onboarder prompt updated (pass `target_name`; never copy a URL SKU into aliases) + `sync-prompt.cjs` re-run. 11 new guard tests (46 total). Web `tsc`/`eslint`/`test:guards 46/46`/`test:parity 6/6`/`next build` green; worker `pytest 413/413`.
- **Prior work:** 2026-05-29 **Phase 29: Redact Google API keys in HTML test fixtures to resolve security alerts.**
  - **Secrets Redaction:** Uniquely mapped and replaced 13 public Google API keys across 5 test fixtures (`bh_dji_cf_challenge.html`, `bh_silver-good-2026-05-21.html`, `bhphotovideo-search-bose.html`, `sabrepc-epyc9255-2026-05-17.html`, `walmart_dji_neo2_parser_gap.html`) with dummy keys (`MOCK_API_KEY_REDACTED_FOR_TESTING_12345` through `MOCK_API_KEY_REDACTED_FOR_TESTING_67890`) to satisfy secret scanner alerts while fully maintaining original key lengths and parser test compatibility.
- **Prior work:** 2026-05-28 **Phase 29 ADR-121: bounded probe-modal loop + host-keyed backfill row de-dup + plan_summary visibility.**
  - **ADR-121:** Server `app/api/onboard/probe/route.ts` now emits `plan_summary` SSE event at start (totalUrls + continueUrls + per-host counts) and `noProgress: true` in the `done` event when a Continue pass finished zero URLs. Client `OnboardChat.tsx`: new `PlanSummary` type + `plan` state, `HostRollup` chip component, header shows `done/total done (attempt N)` while streaming, `upsertBackfill(host, patch)` helper replaces append-on-every-event for `backfill_*` rows, `Continue probing` button gated on `!state.noProgress`, *Save and proceed anyway* turns into blue **Stop and save what we have** with an amber explainer when `noProgress`. Re-labeled "DETAIL-URL BACKFILL" → "Looking for product detail pages on hosts that only have a search URL" + an explicit "Re-trying does not add more searches" note. 4 new guard tests (35 total); web `tsc`/`eslint`/`test:guards 35/35`/`test:parity 6/6`/`next build` clean.
- **Prior work:** 2026-05-28 **Phase 29 ADR-115: save-time probe modal with continue / save-anyway choices.**
  - **ADR-115:** User feedback after ADR-114 shipped: "if we force-finalize, can we add another probe step at the time of saving?" New `POST /api/onboard/probe` SSE endpoint re-runs every `universal_ai_search` URL plus the ADR-076 deterministic detail-URL backfill inside a ~45s server budget, streaming `url_start`/`url_done`/`backfill_*`/`done` events. `OnboardChat.onSave` now opens an inline probe modal that shows per-URL status icons in real time; on `done` it either auto-saves (clean + valid) or shows *Continue probing* / *Save and proceed anyway*. New `bypassForceDetailBackup` option threaded through `validateProfileDraft` and `/api/onboard/save` so the bypass button can reach the commit path; ADR-111 violations downgrade to warnings under that flag (user still sees them; post-save background backfill keeps trying). 2 new guard tests (31 total); web `tsc`/`eslint`/`test:guards 31/31`/`test:parity 6/6`/`next build` clean.
- **Prior work:** 2026-05-28 **Phase 29 ADR-114: server-streamed draft updates keep the onboard preview in sync during tool-use loops.**
  - **ADR-114:** A live 8-vendor DJI Neo 2 re-onboard left the preview pane stuck on `{}` despite the LLM saying it added all 8 vendors. Root cause: `findLatestDraft` reads `<draft>` blocks from assistant text, but Anthropic stops each message at the first `tool_use` — so during a long probe-then-fix-detail-URLs loop the LLM never emits a closing `</draft>`, the client falls back to turn-1's empty stub, and the 50s force-finalize then often returns an incomplete draft. Fixed by (1) server-side extracting `<draft>` from each iteration's `finalMsg` text and streaming a `draft_update` SSE event, (2) also streaming the `draft` argument to every `validate_profile` tool call (which the LLM is already required to do before save), (3) client tracks `latestToolDraft` and prefers it over the message-text scanner, (4) prompt now instructs the LLM to emit `<state>`/`<draft>` blocks INSIDE the text that precedes tool_use (the only place Anthropic will actually emit them on tool-using turns). One new guard test (29 total); web `tsc`/`eslint`/`test:guards 29/29`/`test:parity 6/6`/`next build` clean.
- **Prior work:** 2026-05-28 **Phase 29: Resolve mypy type check error in source_reasons.py**
  - **Mypy type-narrowing:** Fixed a type-checking error where `dominant_rejection` was typed as `str | None` but accessed as a `str` calling `.split()`. Explicitly check that it is not `None` before checking its prefix and calling `.split()`.
- **Prior work:** 2026-05-28 **Phase 29 ADR-113: Auto-forward validation errors to LLM.**
  - **ADR-113:** Modified `OnboardChat.tsx` so that when a profile save fails with a 422 validation error, the UI intercepts it and automatically prompts the LLM (`"I clicked Save but the profile failed validation: ..."`) to fix the draft, rather than showing the error wall to the user.
- **Prior work:** 2026-05-28 **Phase 29 Session B shipped: ADR-112 Scrappey CF-bypass + refined NO_MATCH diagnostic.**
  - **ADR-112:** Tuned `universal_ai.py` to propagate the `render_js` flag down to Scrappey via `"browser": True` in its JSON payload, enabling it to successfully bypass Cloudflare on B&H and Backmarket. Refined the mis-scope diagnostic in `cli.py` to correctly attribute 0-fetched items to "vendor doesn't carry" and 0-passed-all-rejected to "mis-scoped URL".
  - **B-1 & B-3:** Added Scrappey diagnostics logging. Verified ADR-107 fallback for Amazon thin-body recovery is working.
- **Prior work:** 2026-05-28 **Phase 29 Session A shipped: ADR-111 hard-gate `force_detail_backup` + Save-button reset.**
  - **ADR-111:** Promoted `checkForceDetailBackup` from `warnings` to `errors` in `validateProfileDraft` (`web/lib/onboard/validation.ts`). Refactored `checkForceDetailBackup` (`adr067-check.ts`) to take the host set as a parameter — drops the `server-only` + `@/...` alias deps so the offline test suite can exercise it under raw node, mirroring the convention of `checkDetailPreferencePresence`/`checkTitleExcludes`. Reworked the onboarder prompt's "When the profile is ready to save" section so the LLM understands `validate_profile` errors BLOCK save (must fix-and-revalidate before claiming the draft is ready) and gave it the exact recipe for the common amazon/target/walmart violation (web_search → probe_url(detail) → add as 2nd source). Strengthened the single-SKU-exception section with the same enforcement language. Fixed the Save-button-stuck-disabled bug (`OnboardChat.onSubmit` now resets `saveState` from terminal `success`/`error` to `idle` so a follow-up turn re-enables Save). Re-ran `node web/scripts/sync-prompt.cjs`. 6 new guard tests; all 28 guard tests + 6 parity tests pass; web `tsc --noEmit` + `eslint` + `next build` clean; worker 412/412.
- **Prior work:** 2026-05-27 **Phase 29 ADR-107 shipped: Scrappey fallback for known-good thin-body bot walls.**
  - **ADR-107:** Modified `universal_ai.py` so that if `merged` yields 0 listings, the body is thin (`< THIN_BODY_CEILING`), the vendor is marked `alterlab_known_good` in `vendor_quirks.yaml`, and a Scrappey key is available, the adapter automatically tries a Scrappey proxy fetch before falling back to keyword degradation. Re-extracts listings via all tiers on success.
- **Prior work:** 2026-05-27 **Phase 29 ADR-110 shipped + ADR-108 verification completed.**
  - **ADR-110:** Created `turn-budget.ts` to track 50-second wall-clock limits and force finalize. Updated `OnboardChat.tsx` to display an actionable error message upon connection interruption. Relaxed TS SDK types to permit custom max_uses extension for the web_search tool.
  - **ADR-108 live check (completed against prod):** Successfully verified the 3x DJI onboard behavioral consistency test via headless browser automation, eliminating the previous freeze.
- **Prior work:** 2026-05-27 **Phase 29 ADR-106 implemented (parser-gap recall recovery via embedded-state extraction).** Walmart reached a full page but JSON-LD/anchor/full-HTML tiers parsed 0 (`parser_gap`); products live in the Next.js `__NEXT_DATA__` blob that `_strip_to_main_text` strips before the LLM sees it. New deterministic $0 tier `_extract_via_embedded_state` (`universal_ai.py`) parses `searchResult.itemStacks[].items[]`, emits via shared `_jsonld_to_listings`, unioned first in `fetch()`. Live DJI fixture: 0 from JSON-LD → 55 from embedded (target present). 5 regression tests; worker 412/412.
- **Prior work:** 2026-05-27 **Phase 29 ADR-105 + ADR-109 implemented (registry-driven search URLs + honest mis-scope diagnostics).**
  - **ADR-105:** vendor search-URL construction moved out of prompt prose into `vendor_quirks.yaml` as `search_url_template`/`search_url_notes` (Amazon/Walmart/Target/Newegg/BackMarket/Microcenter + 5 book vendors). Microcenter now uses the real keyword param `Ntt=` with a note forbidding the `fq=brand` facet that caused the DJI mis-scope. New deterministic render helpers — `render_search_url` (Py, `vendor_quirks.py`) and `renderSearchUrl` (TS, new `web/lib/onboard/search-url-shared.ts`) — parity-checked via shared fixture `worker/tests/fixtures/search_url/cases.json`. `sync-prompt.cjs` renders templates into the prompt block + emits `SEARCH_URL_TEMPLATES`; the hand-listed URL prose in `onboard_v1.txt` is replaced by a pointer to the rendered block.
  - **ADR-109:** the mis-scope message ("your search URL may be mis-scoped") never reached the React UI because `report_json._source_payload` didn't forward `dominant_rejection` — fixed. Per-source rejection attribution was also keyed on the shared adapter id `universal_ai_search`; now `ai_filter` logs `source_url` per rejection and the new `cli.annotate_dominant_rejections` keys by `match_url`. Regression-tested with the real DJI Microcenter `filter.jsonl` (`dji_microcenter_misscope_filterlog.jsonl`).
  - **Green:** worker 407/407 (ruff+mypy clean); web tsc 0 / eslint 0 / parity 6/6 / guards 22/22 / `next build` ok.
- **Prior work:** 2026-05-27 **Fix onboarder pushback, parallel tool crash, and add dynamic Scrappey fallback.**
  - The onboarder was incorrectly pushing back on "hard vendors" (BackMarket, CentralComputer, etc.) that were recently downgraded to `warning` in ADR-104. `sync-prompt.cjs` was prefixing them with `KNOWN FAILURE (warning)`, causing the LLM to push back. Updated `sync-prompt.cjs` to output `KNOWN QUIRK (warning)`.
  - Fixed an abrupt termination (`[ABRUPT END]`) in `route.ts` caused by the LLM emitting `web_search` and `probe_url` in parallel. `route.ts` now catches all tool uses and returns an explicit error for parallel `web_search` attempts.
  - Refined the `onboard_v1.txt` prompt to instruct the LLM to just add redundant detail URLs silently rather than pausing to ask for permission.
  - Clarified for the user that the generic "Skipped this run because AlterLab was failing on earlier sources" UI message indicates the global 480s circuit breaker tripping (due to earlier sources), not a specific source's AlterLab failure.
  - Configured Amazon to mandate a detail URL (`force_detail_backup: true`) in `vendor_quirks.yaml`.
  - Wired dynamic fallback logic into `universal_ai.py` so that transient AlterLab API failures fall back to Scrappey before exhausting the cascade to basic `httpx`/`curl_cffi` requests.
- **Prior work:** 2026-05-27 **ADR-104 implemented — Fallback scraping API for Cloudflare-walled vendors.** Integrated Scrappey as a Tier 1 bypass mechanism for Cloudflare-walled vendors (Microcenter, B&H, BackMarket, CentralComputer, ServerSupply). Applied `use_scrappey: true` and `proxy_country: UnitedStates` to their `default_alterlab_options` in `vendor_quirks.yaml`. Downgraded these vendors from `severity: blocker` to `warning`, restoring their viability in production searches.
- **Prior work:** 2026-05-27 **ADR-103 implemented — Fix misclassification of Cloudflare challenge pages and revert Datacenter-incompatible vendors.** The `skip_alterlab: true` bypass uses `curl_cffi` to mimic Chrome TLS, which successfully bypasses Cloudflare on local residential IPs but fails in production where the UI worker runs on Datacenter IPs. Cloudflare blocks the Datacenter IP and returns a 5,754-byte challenge page. Because the classifier's `THIN_BODY_CEILING` was set to 5,000 bytes, this challenge page incorrectly fell through the transient check and was reported as `EMPTY_PAGE` (no results). Fixed by raising `THIN_BODY_CEILING` to 15,000 bytes so Cloudflare walls are caught as `TRANSIENT`. Also reverted the `skip_alterlab` vendors (Microcenter, B&H, Backmarket, CentralComputer, ServerSupply) to `severity: blocker` since they are unreachable in production.
- **Prior work:** 2026-05-27 **ADR-102 implemented — Background profile validation via LLM tool.** Added `validate_profile` tool to the onboarder LLM (`web/app/api/onboard/chat/route.ts`). Extracted the save endpoint's validation logic into a shared helper (`web/lib/onboard/validation.ts`) to keep it DRY. The LLM is now instructed to use this tool to validate its draft against schema and deterministic guardrails before emitting the final draft, automatically fixing any issues (like missing `match_aliases` or `title_excludes` substrings) in the background.
- **Prior work:** 2026-05-27 **Implemented `skip_alterlab` to bypass Cloudflare bot-walls via `curl_cffi`.** Vendors that had begun blocking AlterLab's residential proxies with a "Just a moment..." challenge (Microcenter, B&H Photo, Backmarket, CentralComputer, ServerSupply) can now be fetched natively. The `skip_alterlab: true` flag in `vendor_quirks.yaml` routes the fetch directly to the local `curl_cffi` TLS-impersonating client, successfully bypassing the proxy wall and retrieving the full HTML. These vendors were downgraded from `blocker` to `warning` in the `known_failure` list, allowing the onboarder to route them again.
- **Prior work:** 2026-05-27 **ADR-101 implemented — Enforce match_aliases seeding to protect the carry-gate.** Added a deterministic save-time validator that checks for `match_aliases`. If missing, and the system cannot derive a confident family-core model token from `display_name`, the save is outright rejected (HTTP 422), forcing the LLM to provide aliases to proceed. If missing but a confident model token *can* be derived, the save succeeds but a soft warning is surfaced. This guarantees profiles have the necessary context for the ADR-099 carry-gate to function efficiently.
- **Prior work:** 2026-05-27 **ADR-100 implemented — onboarder inclusion policy updated to match the ADR-099 carry-gate.** The onboarder prompt was updated to stop dropping searchable vendors that show 0 matches today. Instead, they are kept as active `universal_ai_search` sources to take advantage of the runtime carry-gate (which costs ~$0 and auto-surfaces stock when it arrives). `sources_pending` is now strictly reserved for unreachable vendors (e.g. hard anti-bot walls where no search URL can be constructed). The mis-scoped URL guard from ADR-098 was preserved (differentiating clean but empty keyword searches from category-node traps). `check-onboard-guards.test.mjs` was updated with tests for these prompt instructions and `sync-prompt.js` regenerated the prompt code.
- **Prior work:** 2026-05-26 **ADR-099 implemented — runtime carry-gate + `match_aliases` + WATCHED status.** A deterministic carry-gate in `universal_ai.fetch()` checks for the product's family-core model token or any `match_aliases` on the fetched page. If absent, it skips extraction (~$0) and reports the source as `WATCHED`. Guardrails ensure aliases are distinctive.
- **Prior work:** 2026-05-27 **Fix run duration and add cost to cards.** Moved `run_started_at` in `worker/src/product_search/cli.py` to the start of the execution so that the CSV file's timestamp correctly captures the start time, making the UI run duration accurate. Updated `web/app/page.tsx` to read `run_cost.total_usd` from the JSON sidecar and display it on the cards next to the duration.

## Current state — **Phase 32 DONE (ADR-135); Phase 31 DONE (ADR-134).** Serper recall works end-to-end (v2 profile `queries` → `adapters/serper.py` → clean `[Listing]` with `buy_url` + Serper-aware URL handling, fixtures + live). NEXT = **Phase 33** per REBUILD_PLAN §10 (eBay wire-in + `vendor_allowlist`/`blocklist`; `selection.vendor_key` already exists). Phase 32 (ADR-135) delivered the v2 run spine + the deterministic edges — original Phase 32 scope: price-sanity + ship-from gate (ADR-131 P1, deterministic MAD-vs-median outlier + country/domain allowlist), diversity/anti-domination selection (`per_vendor_cap` + `max_listings` breadth), type-aware display (`product_type` → relevant `display.attrs`), honest error taxonomy (REBUILD_PLAN §8). The v2 sub-models Phase 32 consumes (`FiltersV2`, `DisplaySpec`, `MatchSpec.variant_strict`) already exist in `profile_v2.py`. Phase 33 = eBay wire-in; Phase 34 = onboarder v2. One phase per session (§10). Eval base: GO (ADR-131), Haiku-4.5 @ temp=0 (ADR-132). Tree green at ADR-135: worker **481/481**, ruff(src)+mypy clean.

> The Phase-29 Session-C defect queue (ADR-117/118/119/120) + ADR-114/115 verification block were **archived 2026-05-30** (moved verbatim to [PROGRESS_ARCHIVE.md](PROGRESS_ARCHIVE.md)) — they are **largely superseded by the rebuild** (ADR-133/134). `variant_strict`/ADR-117 is now a `match.variant_strict` field in `profile_v2.py`; revisit ADR-118/119/120 only if they survive the new (Serper-recall) scope. The Phase-29 work lived on branch `claude/quirky-volta-L3R7L` (web `tsc`/`eslint`/guards 60/60/parity 6/6/build + worker 420/420 green there); the rebuild proceeds on `main`.

## Standing candidates (pick up next)

1. **ADR-074 followup #3** — Target search 0 candidates (largely subsumed by ADR-077, may already be dead).
2. **Schedule & Alerts editor prod verification** (ADR-059/060/061).
3. **Mobile popover layout.**
4. **D4 — magazinesdirect $94 vs live $153/$247 investigation** (carved out of the prior ADR-094 follow-up). The 2026-05-25 extractor was `jsonld`, not detail_llm — likely `lowPrice` from a different SKU on the page, OR a stale fetch + page changed. Capture fixture and run jsonld extraction offline to diagnose.
5. ~~Onboarder Rule Adherence: Guessed Slugs~~ — **CLOSED by ADR-116 (shipped 2026-05-28).** The detail-URL relevance gate + match-alias hallucination guard catch both the slug-guessing class and the ASIN-pollution-of-aliases class.
6. **Onboarder Rule Adherence: Ignored `known_failures`** (ADR-068 gap): LLM places known blockers (like Microcenter) into active sources instead of `sources_pending`. Needs a save-time gate or pipeline step to automatically enforce registry routing.
7. ~~Onboarder Rule Adherence: Missing `match_aliases`~~ — **CLOSED:** ADR-101 (shipped) covers the missing case; **ADR-116 (shipped 2026-05-28)** covers the polluted-aliases (URL-SKU) case.

> **Phase 18 (second-product proof) was RETIRED 2026-05-25 (ADR-086)** — production already proves generality. **Phase 28 (Newegg + B&H search-page recall leaks) closed 2026-05-25 (ADR-087):** Newegg search works post-render (Defect 6 was transient); B&H search is Cloudflare-walled (recall via detail URLs — superseded today by ADR-089, see next). **Phase 24 follow-up (3 ADR-082-flagged hosts) closed 2026-05-25 (ADR-088):** eBay is dedicated-adapter-owned (no render defaults); CentralComputer + ServerSupply are Cloudflare-walled `known_failure`s; the ADR-082 lint now exempts `known_failure`/`dedicated_adapter` hosts and flags the known-good+known_failure contradiction. **Small-defect sweep (3 queued items) closed 2026-05-25 (ADR-089 + ADR-090):** B&H detail is ALSO Cloudflare-walled (promoted to `known_failure: blocker`, supersedes the "detail works" stance in ADR-087); Backmarket persistently CF-walled (also `known_failure: blocker`); the Best Buy curl_cffi HTTP/2 INTERNAL_ERROR root cause was a broken cascade — curl_cffi exceptions propagated past the httpx fallback (ADR-090 fix). **Onboarder robustness paper-cuts closed 2026-05-25 (ADR-091):** silent halt at `max_tokens` 4096 (now 8192 + user-visible "continue" hint); inferred `condition_in` (prompt now forbids inference); misleading baseline-filter line (split universal vs RAM-only); silent drop of `alterlab_known_good` hosts on bare-fetch 5xx (prompt now reads `ok` field as the verdict). **`description:` schema-vs-onboarder gap closed 2026-05-25 (ADR-092):** required `description: str` → optional `str = ""` in both schemas; ai_filter falls back to `display_name` when empty; prompt SHOULD-emit callout preserves the AI-filter context benefit. Closes PROGRESS.md standing candidate #1 (queued since ADR-075). **Run-now UX paper-cut + backend wall-budget tightening closed 2026-05-25 (ADR-093):** UI `POLL_TIMEOUT_MS` 15 → 20 min + graceful post-deadline recovery (final status check rescues just-missed completions; in-flight runs switch to 60s background poll instead of terminal "timed out"); backend `_RUN_BUDGET_SECONDS` 600 → 480 + mid-escalation budget check so an in-flight source's ladder honors the budget instead of running to completion past it. **Subscription / non-RAM price-display correctness closed 2026-05-25 (ADR-094):** `_calculate_total` generalized (RAM arm unchanged + new generic arm so `total_for_target_usd` populates for non-RAM); new `price` report column with as-sold semantics replaced `price_unit` as the default for non-RAM; `DETAIL_SYSTEM_PROMPT` learned to pick the LONGEST subscription term on multi-term pages. D3 live re-verified by user 2026-05-25; D4 (magazinesdirect $94 vs $153/$247) carved off as standing candidate #5. **Remaining onboarder paper-cuts closed 2026-05-26 (ADR-095):** `spec_attrs.required: bool` → `bool = False` default in both Pydantic + TS schemas (closes the stress26-ddr5 422 round-trip class); `build_flags_md` rewritten with a 3-tier description fallback (`profile_desc[flag] → fallback[flag] → fallback[rule_of_flag]`) + bare-bullet on no-match (closes the `- **low_feedback**: (no description)` cosmetic bug that has appeared in every report since ADR-028 — root cause was a key mismatch between rule names and flag labels). Closes PROGRESS standing candidate #4. **ADR-093 + ADR-091 live re-verified 2026-05-26 via Chrome DevTools MCP against prod:** Run-now on `the-week-1yr-subscription` completed in 9m 17s (under 20-min UI poll, 5 sources cleanly skipped under the new 480s budget), auto-refreshed at completion, no terminal "Timed out" message; onboarder multi-vendor sweep turn ("Economist 1yr subscription" throwaway) finished in 3 turns / 2,177 output tokens (well under the new 8192 cap), no freeze, condition_in only added because user explicitly stated it, failed probe routed to sources_pending — both fixes working as designed in prod.

> ADR-075 (`condition_in`) was verified live in prod 2026-05-23 and re-confirmed 2026-05-24 (Phase 23 Part A: `condition_in: [new]` emitted into the saved YAML and visible in the filter log's pass reasons).

## Blockers

None blocking. CI is green (ADR-062 decoupled the worker suite + `validate-profiles` from app-mutable `products/`). Worker suite green at ADR-135 (**481/481**, ruff(src)+mypy clean) on `main`. **Phase 32 DONE (ADR-135) — v2 run pipeline works end-to-end. Next session: implement Phase 33** (REBUILD_PLAN §10 — eBay wire-in + vendor allow/blocklist). Phase 32 delivered (REBUILD_PLAN §5.3): deterministic price-sanity + ship-from/country gate (ADR-131 P1 — MAD-vs-median outlier flag drops the $67.20 DJI anomaly; country/domain allowlist drops/flags foreign offers; no-fabrication-safe), diversity/anti-domination selection (`display.per_vendor_cap` + `max_listings`, "N more from <vendor>" affordance), type-aware display (`product_type` → relevant + populated `display.attrs`), honest error taxonomy (REBUILD_PLAN §8). The Phase-31 v2 sub-models it consumes already exist (`FiltersV2`, `DisplaySpec`, `MatchSpec`). **Did NOT run the CI-parity `uv venv --python 3.12` check** (memory `reference_uv_match_ci_python`) — changes are additive pure-Python with no version-sensitive constructs, mypy+441 tests pass on local 3.13, and CI runs the same suite on push; watch the push CI run.

## Noticed but deferred (live only)

- **ADR-040 auto-demote implementation** — `source_runs` table + streak prune in `_cmd_search`. Deferred by ADR-040 itself; until it lands, 5 dead bose `universal_ai` URLs sit in active `sources` (~$0.011/run; manual demote is the stopgap).
- **ADR-053 deferred items #1–#3** — robust source-error handling / "N source(s) errored" surfacing (now also relevant to `while_below` + `total`-basis zero-listing skips).
- **Scheduled tick #25 (06:11Z) failure-with-nothing-due** — investigate when convenient.
- **No in-app signal for silent external-trigger death** — a redundant independent trigger (e.g. Cloudflare Workers cron) is the only true fix; user was offered it, deferred.
- **Email-on-alert** — deferred to its own ADR + sign-off (push delivery is confirmed working in prod, ADR-057).
- **Phase 5 benchmark fixtures vs live data** — synth picks not re-confirmed against live `anthropic/claude-haiku-4-5` payloads (per ADR-019; not blocking, live data proves Haiku works).

> Older "noticed but deferred" / open-questions / per-phase notes (Phase 10–12 era) live in [PROGRESS_ARCHIVE.md](PROGRESS_ARCHIVE.md); they were stale fossils, not live items.


## 2026-05-27: Fixed time budget exhaustion and dynamic Scrappey fallback
- Converted universal_ai.py global variables to use threading.local() for thread safety
- Added threading.Lock() for circuit breaker logic
- Refactored cli.py _cmd_search to run source fetches in parallel via ThreadPoolExecutor with max_workers=10
- Implemented dynamic Scrappey fallback in _fetch_with_escalation for when all AlterLab rungs result in weak renders (Cloudflare bot walls)
- Cleaned up redundant inline mypy type ignore comment on curl_cffi import to satisfy strict unused-ignore checks

