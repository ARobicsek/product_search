# Progress

**This is the live status file. Every dev session reads it first and updates it last.**
Keep it small (see [SESSION_PROTOCOL.md](SESSION_PROTOCOL.md) — hard cap, archive-on-close).
Full session-by-session history → [PROGRESS_ARCHIVE.md](PROGRESS_ARCHIVE.md) (append-only, NOT read at session start).

## Active phase

- **Closed:** Phases 0–16; **Phase 17** (schedule editor + alerts); **Phase 19** (universal adapter accuracy & vendor reach); **Phase 20** (reliable scheduling trigger); **Phase 22 — Recall reliability under degraded AlterLab + onboarder robustness** (ADR-078/079/080, 2026-05-24); **Phase 23 — Hybrid filter restoration + headless E2E verification** (both Parts A and B closed, 2026-05-24); **Phase 24 — Vendor-quirks coverage audit + Amazon JS-render fix** (ADR-082, 2026-05-24); **Phase 25 — "Explain the zero": classified source-outcome reasons + AlterLab 422 transient retry** (ADR-083/084, 2026-05-24); **Phase 26 — Cross-cutting LIVE stress test & regression sweep** (findings in [STRESS_TEST_26.md](STRESS_TEST_26.md), 2026-05-24); **Phase 27 — Fix the 3 Phase 26 defects + live re-verify** (ADR-085, verification in [STRESS_TEST_27.md](STRESS_TEST_27.m- **ACTIVE ARC — the rebuild (ADR-133, owner-signed [REBUILD_PLAN.md](REBUILD_PLAN.md)): Serper recall + eBay API + Haiku filter, "keep core, redesign edges."** Phase 30 (spike → GO, ADR-131; filter-model = Haiku-4.5@temp=0, ADR-132) DONE. **Phase 31 (Listing generalize + profile v2 + Serper adapter + Serper-aware `single_sku_url` + temp=0) DONE 2026-05-30 (ADR-134).** Phase 32 (price-sanity/ship-from gate + diversity selection + type-aware display + honest error taxonomy + v2 run spine) DONE 2026-05-30 (ADR-135). **Phase 33 (eBay wire-in + vendor allow/blocklist) DONE 2026-05-31 (ADR-136).** **Phase 34 (onboarder v2 wired live → profile v2 + one-shot Serper preview; probe apparatus retired) DONE 2026-05-31 (ADR-137).** **Phase 34 Walkthrough Enhancements (ADR-138) DONE 2026-05-31.** **Phase 35 (alerts `new_vendor_carries`; verify schedule editor + push end-to-end on the new pipeline) DONE 2026-05-31.** **Phase 36 (retire the self-scraping recall tier + v1 onboarder probe apparatus) DONE 2026-05-31 (ADR-139).** **Phase 37 (Smart listing cards for dynamic attributes and UI thumbnails) DONE 2026-06-01.** **The rebuild arc is complete.** **Phase 38 (Amazon recall via DataForSEO — PAYG/structured, off by default) COMPLETE 2026-06-01 (ADR-141 ACCEPTED): 38a worker path + 38b onboarder/deploy/live-verify.**
- **Phase 29 — Recall & reproducibility overhaul** (WOUND DOWN — superseded by the rebuild; ADR-105 through ADR-125 shipped; brief in [PHASES.md](PHASES.md)). Session-C defect queue (ADR-117/118/119/120) archived to [PROGRESS_ARCHIVE.md](PROGRESS_ARCHIVE.md) — revisit only if it survives the new scope. The DONE bullets below are Phase-29/30 lineage, kept for reference.
  - **DONE 2026-05-27: ADR-105 (registry-driven search URLs) + ADR-109 (honest per-source diagnostics)** — the highest-leverage pair for the Microcenter mis-scope class.
  - **DONE 2026-05-27: ADR-106 (parser-gap recall recovery)** — deterministic embedded-state tier for Walmart's `__NEXT_DATA__` grid.
  - **DONE 2026-05-27: ADR-108 (onboard prompt diet)** — `onboard_v1.txt` cut 737→461 lines (−44% by words). See "Most recent work" below.
  - **DONE 2026-05-27: ADR-110** — Onboarder turn budget enforced via `turn-budget.ts`, explicit tool-less finalize, client-side graceful recovery message on interruption. Fixes the silent freeze failure mode.
  - **DONE 2026-05-27: ADR-108 verification** — 3× DJI onboard via Chrome DevTools MCP against prod (`ari-product-search.vercel.app/onboard`). All 3 runs finalized perfectly with no silent timeouts, confirming ADR-110 and ADR-108 stability live.
  - **DONE 2026-05-27: ADR-107** — generalized auto-Scrappey to known-good thin-body vendors. The universal_ai adapter now automatically falls back to Scrappey after AlterLab escalation if it encounters a bot-wall (sub-15KB body) on a vendor marked as `alterlab_known_good`, ensuring that recall is preserved on tough targets without wasting Scrappey credits on normal fetchers.
  - **DONE 2026-05-28: ADR-111 (Session A)** — hard-gated `force_detail_backup` at save (`validateProfileDraft` routes the check to `errors`, not `warnings`), fixed the Save-button-stuck-disabled bug (`OnboardChat.onSubmit` resets `saveState` on follow-up turns), updated the onboarder prompt so `validate_profile` errors are understood to BLOCK save (LLM must fix-and-revalidate, not surface to user). 6 new guard tests; web `tsc`/`eslint`/`next build` clean; worker 412/412.
  - **DONE 2026-05-28: ADR-114** — server-streams `<draft>` JSON to the right pane so the onboard preview stays accurate during long tool-use loops (fixes the "right pane shows `{}` despite 8 vendors added" defect).
  - **DONE 2026-05-28: ADR-115** — save-time probe modal with *Continue probing* / *Save and proceed anyway* and `bypassForceDetailBackup` flag. Final piece for the "force-finalize stranded the user" failure class.
  - **DONE 2026-05-28 (Session C): ADR-121** — Probe modal bounded loop + de-duped backfill rows + plan visibility. Originally PROPOSED as a P3 cosmetic; Session-C code review found two real functional bugs (the loop genuinely never terminated for slow/walled vendors; backfill rows accumulated one-per-attempt because the client never keyed by host). Server now emits `noProgress` in the `done` event when a Continue pass finishes zero URLs; client hides *Continue probing* and offers an explicit **Stop and save what we have** CTA. Backfill events go through a host-keyed `upsertBackfill` so re-skipped vendors render once, not once-per-attempt. New `plan_summary` event seeds a per-host chip roll-up at the top of the modal. 4 new guard tests (35 total).
  - **DONE 2026-05-29: Redacted Google API keys from static test fixtures** — fixed the exposed secrets security alerts by replacing 13 public Google API keys with uniquely mapped dummy keys of matching length (39 characters) in 5 HTML fixture files. Entire test suite (413/413) remains green.
  - **DONE 2026-05-28: ADR-116 (P0)** — detail-URL relevance gate + match_aliases hallucination guard. New pure `detail-title-match.ts` (`titleMatchesTarget`, deterministic token-overlap — rejects the brand-only DJI Transceiver page, accepts siblings/terse titles) wired into `probe-url.ts` (`ProbeResult.detailTitleMatch`; `ok=false` + reason on mismatch). New pure `alias-hallucination-check.ts` (`checkMatchAliasesAgainstHallucinatedSkus` + `extractSkuTokens`) wired into `validation.ts` as a hard error — catches the exact `B0FJ1QH15P`-in-aliases signature. Prompt rule added + `sync-prompt.cjs` re-run. 11 new guard tests (46 total). Closes Standing Candidates #5 + #7.
  - **DONE 2026-05-29: test-slug leak fix** — `ai_filter` mirrors each run's verdicts to `reports/<slug>/<date>.filter.jsonl`; in tests that leaked `reports/test-product`, `reports/test-subscription`, `reports/ddr5-rdimm-256gb` into the working tree on every run. Added a `PRODUCT_SEARCH_REPORTS_DIR` env override (mirrors `PRODUCT_SEARCH_PRODUCTS_DIR`) honored by `_repo_reports_dir()`, plus an autouse session fixture in `conftest.py` pointing it at a tmp dir. Tests now write the per-product log to tmp; real `reports/` stays clean. Worker 413/413, ruff+mypy clean.
  - **DONE 2026-05-29: ADR-122** — onboard-probing UX (from a live DJI re-onboard): save-time probe now REUSES interview probes (chat route streams `probe_result`; `onSave` passes them as `priorResults`; `canReuse` skips re-probing confirmed URLs except force_detail_backup hosts needing a detail URL); probes SEQUENTIALLY fastest-first with a per-URL soft cap so progress is deterministic (was: parallel race → 5/6 "budget exhausted", 0 progress); deterministic "Keep probing the unfinished vendors" affordance on a `turn_truncated` chat-turn timeout; clearer budget-exhausted wording. 6 new guard tests (52 total).
  - **DONE 2026-05-29: ADR-123 (onboarder message clarity) + ADR-124 (run-diagnostic correctness)** — from a live DJI Neo 2 re-onboard + run. **ADR-123:** every save-time validator now carries a plain-English `userMessage` (+ "What to do:") alongside the technical `message`; the LLM still gets the technical text via `validate_profile`, the UI shows the friendly text. De-jargoned the probe-modal header; turned the tiny "Open <slug> anyway →" link into an amber button. +8 guard tests (60 total). **ADR-124:** fixed a real misclassification — `vendor_does_not_carry` (stamped on EVERY `fetched==0` source by `cli.annotate_dominant_rejections`) was checked FIRST in `classify_source_outcome`, making the carry-gate WATCHED + bot-wall/degraded TRANSIENT + PARSER_GAP branches dead code for universal sources. Moved it to last-resort; +5 regression tests (worker 418/418). This is what made the user's Amazon detail source (bot-walled, 0 fetched) report the misleading "Vendor doesn't carry — re-running won't help."
  - **DONE 2026-05-29: ADR-125 (Scrappey recovery for known-good detail pages + Amazon use_scrappey)** — answers "why didn't bot-walled Amazon fall back to Scrappey?" Two gaps: (1) the ADR-107 thin-body Scrappey fallback lived only in the search-union path; an explicit `page_type:detail` source returns `[]` before reaching it. (2) Amazon was `alterlab_known_good` without `use_scrappey`, and its bot-wall (200 + multi-KB body) doesn't trip `_weak_render_reason`. Fix: added a Scrappey recovery to the explicit-detail branch (thin body + known-good + not-already-scrappey → re-fetch + re-extract, `render_js:true`); flagged `amazon.com` `use_scrappey:true`+`render_js:true`+`proxy_country` in the registry (no `skip_alterlab`; AlterLab stays backup; propagates to existing profiles via merge). Repointed the old amazon ADR-107 test to bestbuy.com; +2 tests. Worker 420/420.
  - **DONE 2026-05-29: browser-MCP setup for web-session live testing** — the live-verify task was blocked because the web container had no browser-automation MCP and no vendor/LLM API keys. Fixed by committing `.mcp.json` (chrome-devtools MCP, headless/isolated) + `.claude/hooks/setup-browser.sh` (SessionStart-installs headless Chromium via Playwright, symlinks `/usr/local/bin/chrome-for-cdmcp`) + `.claude/settings.json`. Validated end-to-end this session (MCP init, 29 tools, `navigate_page`+`take_snapshot` on the prod `/onboard` UI). Two container gotchas baked into `.mcp.json`: `--acceptInsecureCerts` (the container does TLS interception → Chrome `ERR_CERT_AUTHORITY_INVALID`) and `--chrome-arg=--no-sandbox` (root). NOTE: MCP loads at session start, so this session can't use it — **the NEXT web session does the actual live test.**
  - **DONE 2026-05-29: live naive-user verification (DJI Neo 2) — findings in [STRESS_TEST_29.md](STRESS_TEST_29.md).** Drove prod end-to-end via Chrome DevTools MCP as a non-technical user (interview → 2 save-probes → save → Run-now → report). **ADR-122/123/124/125 all verified live and PASS.**
  - **DONE 2026-05-29 (planning/strategy session): ADR-130 — Phase 30 recall-layer pivot scoped.**
  - **DONE 2026-05-29: Phase 30 spike Step 3 + GO/NO-GO call → ADR-131 (GO, with 3 prerequisites).**
  - **DONE 2026-05-29: Phase 30 Step 3b/3c — real-model runs + filter-model bake-off (keys provided).**
  - **DONE 2026-05-29: Phase 30 filter-model bake-off → ADR-132.**
  - **DONE 2026-05-30: app-rebuild PLANNING session → ADR-133 + [REBUILD_PLAN.md](REBUILD_PLAN.md) (owner-signed).**
- **Also queued:** **Schedule & Alerts editor prod verification** (ADR-059/060/061). Deferred standing candidate.
- **Most recent work:** 2026-06-01 **Phase 38b DONE — Amazon onboarder + deploy + live verify (Phase 38 COMPLETE; ADR-141 ACCEPTED).** Onboarder v2 (`promptTextV2.ts`) sets `sources.amazon.enabled` (ON physical goods / OFF subscriptions-services-groceries, mirrors eBay); `validation-v2.ts` at-least-one-source gate counts amazon; `.env.example` + both run workflows map `DATAFORSEO_LOGIN`/`DATAFORSEO_PASSWORD`; dead ALTERLAB/SCRAPPEY keys flagged vestigial. +3 web guard tests (70/70), eslint + `next build` clean, worker unchanged (365/365). Live-verified vs real DataForSEO through `run_v2._default_recall` (Amazon-only, live, depth 10): 48 mapped listings, `amazon_error=False`, real `$0.0033` cost. Remaining: owner adds the two GH Actions secrets for prod (off-by-default until then). 2026-06-01 **Phase 38a DONE — Amazon recall worker path** (Steps 2–5, 8): `adapters/amazon.py` (DataForSEO structured PAYG, provider seam, Standard/Live queues, fixture mode, real-cost tracking) + `AmazonSource` in `SourcesV2` (off by default) + wire-in to `run_v2._default_recall`/`run_v2_pipeline`/`classify_run_outcome` (`amazon_unavailable` additive note, per-source independent failure) + flat-fee Amazon spend in the run-cost panel (`_build_run_cost` honors explicit `cost_usd`) + `amazon` marketplace label. +19 offline tests over the 5 committed Step-1 fixtures. Worker **365/365**, ruff(src+tests)+mypy clean. No web changes. **Next: Phase 38b (onboarder toggle + deploy secrets + live verify; then ADR-141 → ACCEPTED).** 2026-06-01 **Phase 38 Step 1 DONE — Amazon recall spike → decisive GO** *(renumbered from 37; a parallel session took Phase 37 for Smart Listing Cards).* Ran `worker/scripts/amazon_spike.py` (committed) against DataForSEO Amazon Products live/advanced on 5 queries (~$0.026 spend). Recall (products/price%/asin%/url%/rating%): DJI Neo 2 124/95/100/100/90; Supermicro H14SSL-N 22/86/100/100/55; lululemon Never Lost Keychain 114/100/100/100/100; DDR5 RDIMM ECC 64GB 113/99/100/100/48; The Week subscription 41/**22**/100/100/51 (back-issues → confirms Amazon-off-for-subscriptions). Exact target ranked top in all 4 physical-goods queries. Cost $0.0033/query (live, depth-100). 5 raw fixtures committed under `worker/tests/fixtures/amazon/` (real CDN image URLs, no base64 bloat). Build gotchas captured in PHASES.md Step 1 (language must be `"English (United States)"`/`en_US` not `"English"`; check task-level status_code; treat 40104/non-20000 as `AmazonAPIError`). **Next: Phase 38a build (Steps 2–5, 8) — `adapters/amazon.py` + `AmazonSource` + wire-in.** 2026-06-01 **Planning (Phase 38 scoped, no code):** Researched + planned **Amazon recall**. Confirmed the seam (one more adapter behind `run_v2._default_recall` + a `SourcesV2` toggle). Decision (ADR-141 PROPOSED): use a **structured PAYG Amazon API — DataForSEO Merchant "Amazon Products," Standard queue (~$0.0006–0.001/search, prepaid PAYG, no subscription)** — **NOT** Scrappey (its cost edge vs structured is illusory, and it re-opens the ADR-139 HTML-parser/CAPTCHA treadmill + wrong-SKU-price risk). Owner constraints honored: PAYG-only (ruled out Scrapingdog/RapidAPI/Unwrangle = subscription; Apify = per-result/pricey), search-only, toggle+query-cap guardrails. Wrote ADR-141 + a full [PHASES.md](PHASES.md) "Phase 38" brief (38a worker recall path / 38b onboarder+deploy). 2026-06-01 **Hotfix:** Fixed a V2 UI layout fallback caused by Github API 1MB contents limits. Serper occasionally returned massive `data:image/webp;base64...` strings that caused V2 JSON payloads to bloat to 1.3MB+, triggering a 403 Forbidden and falling back to markdown rendering. Stripped `data:` URIs from `serper.py`, bringing JSON sidecars back down to ~130KB. 2026-06-01 **Hotfix:** Fixed a V2 pipeline bug where runs always showed "took 0s". `run_v2.py` wasn't capturing `run_started_at` at the start of the execution, causing the saved CSV timestamp and `generated_at` timestamp to be identical. 2026-06-01 **Fix:** Scheduling and Alerting bugs. Migrated the external cron-job trigger to Vercel native cron (`vercel.json`) to resolve silent scheduling deaths, and aliased `VITE_VAPID_PUBLIC_KEY` to `NEXT_PUBLIC_VAPID_PUBLIC_KEY` in `next.config.ts` to fix the known deployment configuration bug where client Push Notifications failed. 2026-06-01 **UI Polish:** Updated the `ResultView` UI to render price anomalies (`price_anomaly_low`, `price_anomaly_high`) as highly visible warning badges with human-readable text ("Suspiciously low price", etc.) instead of raw backend flags. 2026-06-01 **Test Run & Validation:** Verified the new recall maximization strategy on `supermicro-h14ssl-n`. 90 listings were fetched via Serper; 85 junk listings (full servers, old generations) were correctly discarded by the AI filter (Claude Haiku 4.5), confirming that casting a wide net paired with strict AI filtering works perfectly. 2026-06-01 **Hotfix:** Fixed an onboarder UI freeze caused by overlapping streaming/regex bugs in the chat route. The `stripBlocks` regex in `blocks.ts` was missing the `g` flag, truncating multi-turn messages mid-sentence. Added a `\n\n` delimiter in `route.ts` between LLM iterations to fix text merging, and updated `promptTextV2.ts` to instruct the AI to always output a closing sentence so it doesn't end abruptly on a JSON block. 2026-06-01 **Recall Maximization:** Increased default Serper query depth from 40 to 100 in `profile_v2.py`. Updated onboarder prompt (`promptTextV2.ts`) to mandate drafting 3-5 diverse query variations per product, and to encourage setting `variant_strict: false` by default for broader family matching. 2026-06-01 **Hotfix:** Fixed a Next.js Server Component render crash on the homepage (`page.tsx`) caused by a `null` reference when a product has no reports yet. 2026-05-31 **Phase 36 DONE (ADR-139): the self-scraping tier is purged.** Deleted the worker `universal_ai` adapter + `vendor_quirks.py`/`.yaml` + their tests/fixtures; amputated the `universal_ai_search` dispatch branch, cost-panel accounting, and the entire `probe-url`/diagnose command from `cli.py`; removed `universal_ai_search` from `profile.py`'s `KNOWN_SOURCE_IDS`. Deleted the web probe apparatus (`probe/route.ts`, `probe-url.ts`, `probe-and-update.ts`, `gate-universal-ai.ts`, `alterlab-shared.ts`, `sync-prompt.cjs` + its build hooks, `check-alterlab-parity.test.mjs`); removed the v1 draft path from `save/route.ts` (non-v2 drafts now 422). Pruned dead probe-route guard tests. **Scope:** kept the v1 run spine `_cmd_search` + bespoke eBay-storefront adapters + the v1 onboarder *validation/prompt* layer (not the scraping tier; still tested) — a deeper v1-onboarder teardown is left for a future phase. 2026-05-31 **Phase 34 Enhancements (ADR-138) DONE; Phase 35 DONE.**
- **Prior work:** 2026-05-31 **Phase 34 DONE (ADR-137): onboarder v2 wired live.**

## Current state — **Phase 38 COMPLETE (38a + 38b DONE): Amazon recall via DataForSEO shipped end-to-end; ADR-141 ACCEPTED.** 38a (worker path) shipped `adapters/amazon.py` + `AmazonSource` + the `run_v2` wire-in (off by default). **38b (this session — onboarder + deploy + live verify):** `promptTextV2.ts` teaches the v2 onboarder to set `sources.amazon.enabled` (ON for physical goods on Amazon, OFF for subscriptions/services/groceries — mirrors eBay; notes Amazon US is absent from Google Shopping; draft-shape + `<state>` ledger include `amazon`; min-qty caveat names Amazon). `validation-v2.ts` at-least-one-source gate now counts `sources.amazon`. `.env.example` gained the DataForSEO section + flagged the dead ALTERLAB/SCRAPPEY keys vestigial. `DATAFORSEO_LOGIN`/`DATAFORSEO_PASSWORD` mapped into the worker env in **both** `search-on-demand.yml` + `search-scheduled.yml`. **Live-verified** against real DataForSEO via `run_v2._default_recall` (Amazon-only, priority=live, depth=10): 48 mapped listings, `amazon_error=False`, real `$0.0033` in `amazon_cost_usd`. Web green: guards **70/70** (+3), eslint clean, `next build` clean; worker unchanged (**365/365**). The Serper-only live preview is unchanged (Amazon preview deferred).

**NEXT: Phase 39 — AI-filter cost reduction (PROPOSED, ADR-142; scoped 2026-06-01, not yet built).** Prompt-cache the Haiku filter's ~16K-token rules system prompt (re-sent per 50-listing batch today, no caching) + trim per-listing verdict output (drop never-surfaced pass-reasons). Worker-only; keep `temperature=0` determinism (byte-identical survivor set). Est. ~$0.22→~$0.13 per filter-heavy run. Full brief: PHASES.md "Phase 39". *(Triggered by the DJI run below: the filter was $0.219 of the $0.224 total.)* **Amazon recall is LIVE + prod-verified (2026-06-01):** owner funded the DataForSEO account + added the two GH Actions secrets; an on-demand Run-now on `dji-neo-2-motion-fly-more-combo` returned outcome `ok`, Amazon supplied 11/65 survivors (cheapest overall $479 Amazon), `amazon_recall` cost $0.005, no `amazon_unavailable`, standard queue finished inside the 6-min poll window. **Onboarder filter affordances (for reference):** vendor_allow/blocklist + title_excludes + condition/stock/min_qty (all deterministic, pre-LLM) — but a *specific* third-party Amazon seller can't be blocked (every Amazon row's seller is labeled "Amazon").

> The Phase-29 Session-C defect queue (ADR-117/118/119/120) + ADR-114/115 verification block were **archived 2026-05-30** (moved verbatim to [PROGRESS_ARCHIVE.md](PROGRESS_ARCHIVE.md)) — they are **largely superseded by the rebuild** (ADR-133/134). `variant_strict`/ADR-117 is now a `match.variant_strict` field in `profile_v2.py`; revisit ADR-118/119/120 only if they survive the new (Serper-recall) scope. The Phase-29 work lived on branch `claude/quirky-volta-L3R7L` (web `tsc`/`eslint`/guards 60/60/parity 6/6/build + worker 420/420 green there); the rebuild proceeds on `main`.
## Standing candidates (pick up next)

1. **ADR-074 followup #3** — Target search 0 candidates (largely subsumed by ADR-077, may already be dead).
2. **Mobile popover layout.**
3. **D4 — magazinesdirect $94 vs live $153/$247 investigation** (carved out of the prior ADR-094 follow-up). The 2026-05-25 extractor was `jsonld`, not detail_llm — likely `lowPrice` from a different SKU on the page, OR a stale fetch + page changed. Capture fixture and run jsonld extraction offline to diagnose.
4. **Onboarder Rule Adherence: Ignored `known_failures`** (ADR-068 gap): LLM places known blockers (like Microcenter) into active sources instead of `sources_pending`. Needs a save-time gate or pipeline step to automatically enforce registry routing.

> **Phase 18 (second-product proof) was RETIRED 2026-05-25 (ADR-086)** — production already proves generality. **Phase 28 (Newegg + B&H search-page recall leaks) closed 2026-05-25 (ADR-087):** Newegg search works post-render (Defect 6 was transient); B&H search is Cloudflare-walled (recall via detail URLs — superseded today by ADR-089, see next). **Phase 24 follow-up (3 ADR-082-flagged hosts) closed 2026-05-25 (ADR-088):** eBay is dedicated-adapter-owned (no render defaults); CentralComputer + ServerSupply are Cloudflare-walled `known_failure`s; the ADR-082 lint now exempts `known_failure`/`dedicated_adapter` hosts and flags the known-good+known_failure contradiction. **Small-defect sweep (3 queued items) closed 2026-05-25 (ADR-089 + ADR-090):** B&H detail is ALSO Cloudflare-walled (promoted to `known_failure: blocker`, supersedes the "detail works" stance in ADR-087); Backmarket persistently CF-walled (also `known_failure: blocker`); the Best Buy curl_cffi HTTP/2 INTERNAL_ERROR root cause was a broken cascade — curl_cffi exceptions propagated past the httpx fallback (ADR-090 fix). **Onboarder robustness paper-cuts closed 2026-05-25 (ADR-091):** silent halt at `max_tokens` 4096 (now 8192 + user-visible "continue" hint); inferred `condition_in` (prompt now forbids inference); misleading baseline-filter line (split universal vs RAM-only); silent drop of `alterlab_known_good` hosts on bare-fetch 5xx (prompt now reads `ok` field as the verdict). **`description:` schema-vs-onboarder gap closed 2026-05-25 (ADR-092):** required `description: str` → optional `str = ""` in both schemas; ai_filter falls back to `display_name` when empty; prompt SHOULD-emit callout preserves the AI-filter context benefit. Closes PROGRESS.md standing candidate #1 (queued since ADR-075). **Run-now UX paper-cut + backend wall-budget tightening closed 2026-05-25 (ADR-093):** UI `POLL_TIMEOUT_MS` 15 → 20 min + graceful post-deadline recovery (final status check rescues just-missed completions; in-flight runs switch to 60s background poll instead of terminal "timed out"); backend `_RUN_BUDGET_SECONDS` 600 → 480 + mid-escalation budget check so an in-flight source's ladder honors the budget instead of running to completion past it. **Subscription / non-RAM price-display correctness closed 2026-05-25 (ADR-094):** `_calculate_total` generalized (RAM arm unchanged + new generic arm so `total_for_target_usd` populates for non-RAM); new `price` report column with as-sold semantics replaced `price_unit` as the default for non-RAM; `DETAIL_SYSTEM_PROMPT` learned to pick the LONGEST subscription term on multi-term pages. D3 live re-verified by user 2026-05-25; D4 (magazinesdirect $94 vs $153/$247) carved off as standing candidate #5. **Remaining onboarder paper-cuts closed 2026-05-26 (ADR-095):** `spec_attrs.required: bool` → `bool = False` default in both Pydantic + TS schemas (closes the stress26-ddr5 422 round-trip class); `build_flags_md` rewritten with a 3-tier description fallback (`profile_desc[flag] → fallback[flag] → fallback[rule_of_flag]`) + bare-bullet on no-match (closes the `- **low_feedback**: (no description)` cosmetic bug that has appeared in every report since ADR-028 — root cause was a key mismatch between rule names and flag labels). Closes PROGRESS standing candidate #4. **ADR-093 + ADR-091 live re-verified 2026-05-26 via Chrome DevTools MCP against prod:** Run-now on `the-week-1yr-subscription` completed in 9m 17s (under 20-min UI poll, 5 sources cleanly skipped under the new 480s budget), auto-refreshed at completion, no terminal "Timed out" message; onboarder multi-vendor sweep turn ("Economist 1yr subscription" throwaway) finished in 3 turns / 2,177 output tokens (well under the new 8192 cap), no freeze, condition_in only added because user explicitly stated it, failed probe routed to sources_pending — both fixes working as designed in prod.

> ADR-075 (`condition_in`) was verified live in prod 2026-05-23 and re-confirmed 2026-05-24 (Phase 23 Part A: `condition_in: [new]` emitted into the saved YAML and visible in the filter log's pass reasons).

## Blockers

None blocking. CI is green (ADR-062 decoupled the worker suite + `validate-profiles` from app-mutable `products/`). Worker suite green (**365/365**, ruff(src+tests)+mypy clean) on `main`; web green (`test:guards` 67/67, `test:parity` 4/4, `tsc`/`eslint`/`next build` clean). **Phase 36 DONE (ADR-139) — the self-scraping tier + v1 probe apparatus are purged; the rebuild arc (Phases 30–36) is complete.** **Serper deploy prereqs done:** `SERPER_API_KEY` is set in all three stores (local, Vercel, GH Actions secret); the run workflows map it into the worker env (`b0d024a`). **Phase 38 Amazon recall is LIVE in prod (verified 2026-06-01):** both run workflows map `DATAFORSEO_LOGIN`/`DATAFORSEO_PASSWORD` into the worker env; the owner funded the DataForSEO account + added both GH Actions secrets; a prod on-demand Run-now (`dji-neo-2-motion-fly-more-combo`) recalled Amazon end-to-end (outcome `ok`, 11/65 survivors, $0.005, no `amazon_unavailable`). Amazon stays off-by-default until a profile enables it.

## Noticed but deferred (live only)

- **Worker pytest re-leaks test-slug reports into the working tree** — running the worker suite dirties `reports/test-product/2026-05-31.filter.jsonl` + `reports/test-v2-alerts/*` (a v2 test isn't honoring `PRODUCT_SEARCH_REPORTS_DIR` for those slugs, despite the ADR-062-era tmp-dir fixture). Pre-existing; `git restore` + delete the untracked CSVs before committing. Real fix: route the leaking test's report writes through the tmp override.
- **Retire the rest of the v1 onboarder (post-Phase-36 candidate)** — Phase 36 removed the scraping/probe tier but deliberately KEPT the v1 onboarder *validation/prompt* layer (`web/lib/onboard/validation.ts` + its check modules, the frozen `promptText.ts`/`vendor-quirks-data.ts`, `detail-title-match.ts`, `search-url-shared.ts`; worker `onboard_v1.txt`) and the v1 run spine `_cmd_search` + the now-inert `universal_ai_search` attribution branches in `synthesizer.py`/`report_json.py`/`ai_filter.py`/`cli.py`. All dead at runtime (v2 uses `validation-v2.ts`/`promptTextV2`/`run_v2`) but still covered by the v1 guard tests. A future phase can delete them once it confirms no v1 profiles remain.
- **Honor `min_quantity` for eBay listings (Phase 33 follow-up)** — eBay now carries real `quantity_available`, but the v2 pipeline does not yet reject eBay listings below `filters.min_quantity` (the `degraded_attr` note still fires whenever `min_quantity` is set). Deliberately out of Phase-33 scope (CLAUDE.md "don't add features the task didn't ask for"). A small honest filter — reject eBay listings with known qty < min, pass Serper's unknown-qty listings, degrade-note only when an unknown-qty survivor remains — completes REBUILD_PLAN §5.5. `profile_v2_filter.to_filter_profile` intentionally does NOT map `min_quantity` today.
- **ADR-040 auto-demote implementation** — `source_runs` table + streak prune in `_cmd_search`. Deferred by ADR-040 itself; until it lands, 5 dead bose `universal_ai` URLs sit in active `sources` (~$0.011/run; manual demote is the stopgap).
- **ADR-053 deferred items #1–#3** — robust source-error handling / "N source(s) errored" surfacing (now also relevant to `while_below` + `total`-basis zero-listing skips).
- ~~Scheduled tick #25 (06:11Z) failure-with-nothing-due~~ — fixed 2026-06-01 via ADR-140 (Vercel native cron).
- ~~No in-app signal for silent external-trigger death~~ — fixed 2026-06-01 by adopting Vercel native cron in `vercel.json` (ADR-140).
- **Email-on-alert** — deferred to its own ADR + sign-off (push delivery is confirmed working in prod, ADR-057).
- **Phase 5 benchmark fixtures vs live data** — synth picks not re-confirmed against live `anthropic/claude-haiku-4-5` payloads (per ADR-019; not blocking, live data proves Haiku works).

> Older "noticed but deferred" / open-questions / per-phase notes (Phase 10–12 era) live in [PROGRESS_ARCHIVE.md](PROGRESS_ARCHIVE.md); they were stale fossils, not live items.


## 2026-05-27: Fixed time budget exhaustion and dynamic Scrappey fallback
- Converted universal_ai.py global variables to use threading.local() for thread safety
- Added threading.Lock() for circuit breaker logic
- Refactored cli.py _cmd_search to run source fetches in parallel via ThreadPoolExecutor with max_workers=10
- Implemented dynamic Scrappey fallback in _fetch_with_escalation for when all AlterLab rungs result in weak renders (Cloudflare bot walls)
- Cleaned up redundant inline mypy type ignore comment on curl_cffi import to satisfy strict unused-ignore checks

